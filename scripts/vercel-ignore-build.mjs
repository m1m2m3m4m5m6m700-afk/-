// Vercel Git auto-deploys are intentionally paused to prevent unnecessary
// builds/deployments while the project is being stabilized.
// Manual Vercel deployments remain possible outside the Git integration.
console.log("Vercel automatic Git deployment skipped: auto-sync is paused.");
process.exit(0);
