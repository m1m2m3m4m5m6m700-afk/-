import { existsSync } from "node:fs";
import { main, json, files, text, rel } from "./_core.mjs";
await main("check-regression-guard",()=>{const path="REGRESSION_RULES.json";if(!existsSync(path))return{severity:"CRITICAL",message:"REGRESSION_RULES.json missing",findings:[path]};const cfg=json(path);const findings=[];if(!Number.isInteger(cfg.version)||!Array.isArray(cfg.rules))findings.push("Schema requires integer version and rules array");for(const rule of cfg.rules||[]){if(!rule.id||typeof rule.pattern!=="string")findings.push(`Invalid rule schema: ${JSON.stringify(rule)}`);else{let re;try{re=new RegExp(rule.pattern,rule.flags||"")}catch{findings.push(`Invalid regex in rule ${rule.id}`);continue}for(const f of files("src",/\.(ts|tsx|js|jsx|mjs)$/))if(re.test(text(f)))findings.push(`${rel(f)} matches regression rule ${rule.id}`)}}return{severity:findings.length?"CRITICAL":"INFO",message:findings.length?"Regression guard triggered":"Regression guard PASS",findings};});