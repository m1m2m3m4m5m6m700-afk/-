import { existsSync } from "node:fs";
import { main, json } from "./_core.mjs";
await main("check-lockfile", () => { if(!existsSync("package.json")||!existsSync("package-lock.json")) return {severity:"CRITICAL",message:"Lockfile inputs missing",findings:["package.json/package-lock.json required"]}; const p=json("package.json"), l=json("package-lock.json"), root=l.packages?.[""]??{}; const findings=[]; if(root.name!==p.name) findings.push(`name mismatch: ${root.name} vs ${p.name}`); if(root.version!==p.version) findings.push(`version mismatch: ${root.version} vs ${p.version}`); for(const k of ["dependencies","devDependencies","optionalDependencies","overrides"]){const a=JSON.stringify(p[k]||{}),b=JSON.stringify(root[k]||{});if(a!==b) findings.push(`${k} differ between package.json and lockfile root`);} return {severity:findings.length?"CRITICAL":"INFO",message:findings.length?"package manifest and lockfile are desynchronized":"Lockfile contract PASS",findings}; });