export function verify(plan){const e=[];if(plan?.status!=='planned')e.push('manual review');for(const c of plan?.changes??[]){if(c.file.includes('..')||c.file.startsWith('/'))e.push('unsafe path');if(c.file==='.github/workflows/release-certification.yml')e.push('protected release workflow');}const p=(plan?.changes??[]).some(c=>c.file==='package.json');const l=(plan?.changes??[]).some(c=>c.file==='package-lock.json');if(p!==l&&plan?.category==='DEPENDENCY')e.push('package and lockfile must move together');return {valid:e.length===0,approved:e.length===0,errors:e};}
