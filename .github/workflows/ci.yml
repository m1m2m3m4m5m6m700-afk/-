name: CI

on:
  push:
    branches: [main, experimental]
    paths:
      - "src/**"
      - "scripts/**"
      - ".github/scripts/**"
      - "tests/**"
      - "public/**"
      - "package.json"
      - "package-lock.json"
      - "vite.config.ts"
      - "tsconfig*.json"
      - ".nvmrc"
      - ".github/workflows/**"
  pull_request:
    branches: [main, experimental]
    paths:
      - "src/**"
      - "scripts/**"
      - ".github/scripts/**"
      - "tests/**"
      - "public/**"
      - "package.json"
      - "package-lock.json"
      - "vite.config.ts"
      - "tsconfig*.json"
      - ".nvmrc"
      - ".github/workflows/**"
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    name: Full Verification (${{ github.ref_name }})
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - name: Validate CI contract
        run: npm run validate-ci-contract
      - name: Validate dependency contract
        run: npm run validate:dependencies
      - name: Install dependencies
        run: npm ci
      - name: Lockfile unchanged after install
        run: git diff --exit-code -- package.json package-lock.json
      - name: Install Chromium for operational tests
        run: npx playwright install --with-deps chromium
      - name: Run canonical verification gate
        shell: bash
        run: |
          set -o pipefail
          mkdir -p diagnostics
          echo "Starting npm run verify at $(date -u +%FT%TZ)"
          timeout --signal=TERM --kill-after=30s 35m npm run verify 2>&1 | tee diagnostics/npm-verify.log
      - name: Upload browser report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flixo-playwright-report-${{ github.ref_name }}
          path: playwright-report
          if-no-files-found: ignore
          retention-days: 7
      - name: Upload canonical verification diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flixo-canonical-verify-diagnostics-${{ github.run_id }}
          path: |
            diagnostics/npm-verify.log
            test-results/
            playwright-report/
          if-no-files-found: ignore
          retention-days: 14
      - name: Production dependency audit
        run: npm run audit:production
      - name: Upload diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-diagnostics-${{ github.run_id }}
          path: |
            ~/.npm/_logs/
            diagnostics/
            test-results/
            playwright-report/
          if-no-files-found: ignore
          retention-days: 14