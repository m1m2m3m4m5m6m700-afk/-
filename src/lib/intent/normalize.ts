import { includesTerm as coreIncludesTerm, normalizeIntent as coreNormalizeIntent } from './normalize-core.mjs';

export const normalizeIntent = coreNormalizeIntent;
export const includesTerm = coreIncludesTerm;
